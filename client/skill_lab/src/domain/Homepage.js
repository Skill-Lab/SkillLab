import React from 'react'

export default function Homepage() {
    return (
        // {user ? <Redirect to="/home" /> : <Redirect to="/" />}
        <div>
            Homepage
        </div>
    )
}
